// /processors/messages/sendMenu.mjs

import { config } from '../../../config.mjs'
import hasMatchingKeywords from '../../utils/hasMatchingKeywords.mjs';
import logger from '../../utils/logger.mjs'

export async function sendMenu(message, messageMeta) {
    try {
        const keywords = ["!قائمة", "!قائمه", "!القائمة", "!القائمه", "!menu", '!list'];
        if (!hasMatchingKeywords(message.body, keywords)) return

        // بداية الرسالة
        let menuText = `مرحباً ${messageMeta.pushname}! إليك قائمة الأوامر المتاحة:\n\n`;

        // 📌 قسم الملصقات
        menuText += `🖼 *أوامر الملصقات والميديا:*\n`;
        menuText += `1. *!ملصق*: لتحويل صورة أو فيديو إلى ملصق. \n📌 *مثال:* أرسل الصورة او الفيديو مع كتابة الأمر في الوصف مثل: !ملصق.\n`;
        menuText += `2. *!خلفية*: لإزالة الخلفية من الصورة. \n📌 *مثال:* أرسل الصورة مع كتابة الأمر في الوصف مثل: !خلفية.\n`;
        menuText += `3. *!دائرة*: لتحويل الصورة إلى شكل دائرة مع خلفية شفافة. \n📌 *مثال:* أرسل الصورة مع كتابة الأمر في الوصف مثل: !دائرة.\n`;
        menuText += `4. *!إرجاع*: لتحويل الملصق إلى صورة أو فيديو. \n📌 *ملاحظة:* يجب أن تكون الرسالة المُراد إرجاعها هي *رد* على الملصق.\n`;
        menuText += `5. *!اسرق*: لسرقة الملصقات. \n📌 *ملاحظة:* يجب أن يكون الأمر *رد* على الملصق الذي ترغب بسرقته.\n`;
        menuText += `6. *!كتابة [رقم-النمط]*: إنشاء ملصق نصي بـ 10 أنماط فنية. \n📌 *مثال:* !كتابة3 مرحباً.\n`;
        menuText += `7. *!صورة [رقم-النمط]*: تحويل صورة إلى ملصق مع نص بـ 10 أنماط فنية. \n📌 *مثال:* !صورة5 مرحباً.\n`;
        menuText += `8. *!رقمي*: لإنشاء ملصق برقم الهاتف الخاص بك.\n📌 *مثال:* أرسل الرقم مع كتابة الأمر مثل: !رقمي.\n`;
        menuText += `9. *!صلاة*: يرسل ملصق بوقت الصلاة القادمة بتوقيت مكة المكرمة.\n`;
        menuText += `10. *!تاريخ*: يرسل ملصق يحتوي على التاريخ الهجري والميلادي الحالي.\n`;
        menuText += `11. *!توقيع [النص]*: لإنشاء ملصق توقيع مميز بـ *5 أنماط مختلفة*.\n📌 *مثال:* !توقيع4 فلان الفلاني.\n`;
        menuText += `12. *!فيديو [النص]*: لإنشاء ملصق متحرك مع نص. \n📌 *مثال:* !فيديو مرحبا بالجميع.\n`;

        // إضافة الأوامر الجديدة مع الأمثلة
        menuText += `13. *!simg [الكلمة المفتاحية]*: للبحث عن صورة من جوجل وتحويلها إلى ملصق. \n📌 *مثال:* !simg قطط.\n`;
        menuText += `14. *!sgif [الكلمة المفتاحية]*: للبحث عن GIF من جوجل وتحويله إلى ملصق متحرك. \n📌 *مثال:* !sgif مرحبا.\n\n`;

        // 📌 قسم الأوامر الأخرى
        menuText += `🔹 *أوامر أخرى:*\n`;
        menuText += `15. *!سؤال*: لطرح سؤال إسلامي بشكل عشوائي في جميع المجالات.\n`;
        menuText += `16. *!إجابة*: للرد على السؤال ومعرفة الإجابة.\n`;
        menuText += `17. *!صوت [1-15]*: لتطبيق تأثير صوتي على ملف صوتي أو فيديو.\n`;
        menuText += `18. *!تقويم*: للحصول على التقويم الهجري والميلادي لهذا اليوم.\n\n`;

        // ملاحظات هامة
        menuText += `📌 *ملاحظات هامة:*\n\n`;
        menuText += `- تأكد من كتابة الأمر مع الرمز "!" مباشرة، مثل: !ملصق وليس ملصق فقط.\n`;
        menuText += `- عند استخدام الأوامر مثل !ملصق و !دائرة و !خلفية و !إرجاع و !اسرق و !فيديو: يجب أن تُرسل كـ *رد على الرسالة* أو تكتب الأمر في _وصف الميديا_.\n\n`;

        // إضافة الرسالة الختامية
        menuText += `اختر الأمر الذي تود استخدامه! 😄`;

        // إرسال الرسالة
        return await message.reply(menuText);
    } catch (error) {
        logger.error('Error sending menu response:', error);
    }
}
